Use Location Map For Trip

# Step 1

- Crear mongo amb "location-parks.js" public a la DB (sense login)
- En comptes de escriure locatios directe -> fer una request "onMount"
  -> useEffect( () => ..., [])

fetch a mongo

# Step 2

Login (to be discussed)
